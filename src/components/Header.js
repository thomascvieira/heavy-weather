
// const Header = ({toggleInput, zipCode, title}) => {
//   !toggleInput ? {title} = 'Heavy Weather' : {title} = 'Weather for '+{zipCode}
//     return (
//       <header>
//         <h1 className="header">{title}</h1>
//       </header>
//     )
// }

const Header = () => {
      return (
        <header>
          <h1 className="header">Heavy Weather</h1>
        </header>
      )
  }

export default Header
