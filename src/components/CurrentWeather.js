const CurrentWeather = ({weatherData}) => {
    // let iconClass = weatherData["weather"][0].id
    return (
        <div>
            <h3>Current weather information</h3>
            {/* <i className={`wi wi-owm-${iconClass} currentIcon`}></i> */}
        </div>
    )
}

export default CurrentWeather
